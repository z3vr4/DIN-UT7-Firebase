name: ut7_firebase_api
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  go_router: ^14.0.0
  ui_components:
    path: packages/ui_components
  firebase_core: ^4.3.0
  http: ^1.6.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  fl_chart: ^1.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
